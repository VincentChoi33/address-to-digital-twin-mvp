{
  "name": "address-to-digital-twin-preview-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "npm run lint && vite build",
    "sample:sadang": "tsx src/pipeline/runAddressTwin.ts --sample sadang",
    "export:sadang": "tsx src/pipeline/runAddressTwin.ts --sample sadang",
    "prepare:deploy": "npm run export:sadang && npm run build && rm -rf dist/samples && mkdir -p dist/samples && cp -R src/samples/sadang_317_6 dist/samples/",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/three": "^0.160.0",
    "playwright": "^1.59.1",
    "tsx": "^4.7.1",
    "typescript": "^5.4.2",
    "vite": "^8.0.10"
  }
}
