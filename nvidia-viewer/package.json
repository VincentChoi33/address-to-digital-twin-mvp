{
  "name": "address-twin-nvidia-ovstream-viewer",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "probe:first-frame": "node scripts/probe-first-frame.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@nvidia/ov-web-rtc": "^6.4.4"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.9.3",
    "vite": "^8.1.5"
  }
}
